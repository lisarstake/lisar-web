/**
 * Publication API Service Interface
 * Defines the contract for publication-related API operations
 * 
 * Note: The interface is exported from types.ts for consistency
 */

// Re-export the interface from types.ts
export { IPublicationApiService } from './types';
