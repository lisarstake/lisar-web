# Web App (placeholder)

This is a placeholder for the public website app in the monorepo. Your existing app remains at the repository root to avoid breaking changes. When ready, we will move it into `apps/web` with path alias and config updates.
