// Re-export from mock data for backward compatibility
// TODO: Remove this file once all components are updated to use the API service
export { mockOrchestrators as orchestrators, mockWalletData as walletData } from '../mock/orchestrators';